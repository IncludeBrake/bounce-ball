let x = 0;
let y = 0;
let speed = 3;

function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);
  x = x + speed;
  y = y + speed;
  if (x > width || x < 0) {
    speed = -speed;
  }
  if (y > height || y < 0) {
    speed = -speed;
  }
  ellipse(x, y, 50, 50);
}
